spring:
  application:
    name: SampleCNWeather

mvf4ms:
  version: 2.2.0

server:
  port: 8080
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://${REGISTRY}/eureka
